import {Anton, Viga} from 'next/font/google';

export const anton = Anton({
  weight: '400',
  subsets: ['latin'],
  variable: '--font-anton',
})

export const viga = Viga({
    weight: '400',
    subsets: ['latin'],
    variable: '--font-viga',
})